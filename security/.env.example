# Kimi Ecosystem Security Environment Variables
# Copy this file to .env and fill in your secrets

# Generate secrets with: openssl rand -base64 32
SECURITY_AUDITOR_JWT_SECRET=change-me-in-production
SYSADMIN_AI_JWT_SECRET=change-me-in-production
CONVERGENCE_LOOP_JWT_SECRET=change-me-in-production
DASHBOARD_JWT_SECRET=change-me-in-production

# TLS Certificate Paths (auto-generated by setup.sh)
# These are mounted as volumes in docker-compose.yml
# SECURITY_AUDITOR_TLS_CERT=./certs/security-auditor/tls.crt
# SECURITY_AUDITOR_TLS_KEY=./certs/security-auditor/tls.key
# SECURITY_AUDITOR_TLS_CA=./certs/ca.crt

# API Keys (generate with: python shared/generate_api_key.py)
# Format: key_id:hash,key_id2:hash2
# SECURITY_AUDITOR_API_KEYS=default:abc123...

# Database (for production)
# DASHBOARD_DB_URL=postgresql://user:pass@db/kimi_dashboard

# Redis (for production session storage)
# REDIS_URL=redis://redis:6379/0

# Logging
LOG_LEVEL=info
LOG_FORMAT=json